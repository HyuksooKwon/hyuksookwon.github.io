# Files to remove from the old Quarto version

After copying the new files, the following old files can be deleted:

- `_quarto.yml`
- `index.qmd`
- `research.qmd`
- `presentations.qmd`
- `teaching.qmd`
- `styles.css`
- `LINK_AUDIT.md`
- `images/README.txt`

The new workflow publishes only the static HTML files, so leaving the old files temporarily will not break the site. Deleting them simply keeps the repository clean.
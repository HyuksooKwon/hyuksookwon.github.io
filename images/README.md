The draft currently loads the existing public profile image remotely.

For a fully self-contained site, add the preferred portrait here as `profile.jpg`, then change the image source in `index.qmd` from the remote URL to `images/profile.jpg`.
